---
import { getCollection } from "astro:content";
import DirectoryLayout from "../../layouts/DirectoryLayout.astro";

const allEntries = await getCollection("atlas");
---

<DirectoryLayout
  title="Directorio — TECH_ATLAS"
  entries={allEntries}
  cityEntries={allEntries}
/>
